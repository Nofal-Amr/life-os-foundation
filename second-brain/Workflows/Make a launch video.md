---
tags:
  - workflow
updated: 2026-09-22
---
# Make a launch video

Brag skill + Hyperframes: plan → brief → `build.mjs` → `npx hyperframes check` → render → poster as frame 0. Needs FFmpeg (installed via winget). Cuts in `brag-output-*` (kept out of git).

**Related**: [[Launch videos]]
